<%= form_for(@employer_profile) do |f| %>
  <% if @employer_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employer_profile.errors.count, "error") %> prohibited this employer_profile from being saved:</h2>

      <ul>
      <% @employer_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :bio %><br />
    <%= f.text_field :bio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
