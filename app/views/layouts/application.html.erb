<!DOCTYPE html>
<html>
<head>
  	<title>Raison Mentors | <%= yield :title %></title>
	<meta charset="utf-8">
	<meta name="description" content="Your description">
    <meta name="keywords" content="Your keywords">
	
	<%= javascript_include_tag "jquery-1.7.1.min"%>
	
	<%= stylesheet_link_tag "application" %>
	<%= javascript_include_tag "application" %>
  	<%= csrf_meta_tags %>
</head>
<body>
	<p class="notice"><%= flash[:notice] %></p>
	<p class="alert"><%= flash[:alert] %></p>
	<div class="bg-main"> <!--start background main-->
		<header>
			<div class="container_24">
				<div class="wrapper">
					<div class="grid_17">
						<h1><%=link_to "Home", root_path %></h1>
					</div>
					<% if member_signed_in? %>
						<div class="logged_in_controls"> 
							<h3><%= link_to "dashboard", :controller => "members", :action => "dashboard" %></h3> 
						</div>
					<% else if client_signed_in? %>
						<div class="logged_in_controls"> 
							<h3><%= link_to "dashboard", :controller => "clients", :action => "dashboard" %></h3> 
						</div>
					<% end %>
					<% end %>
				</div>
			</div>
			<%= render :partial => 'shared/navigation' %>
		</header>
		<section class="padsection"> <!-- wrapper -->
			<div class="container_24"><!-- container -->
				<%= yield %>
			</div> <!--end container-->
		</section> <!--end wrapper-->
	</div> <!--end background main-->
	<%= render :partial => 'shared/footer' %>
</body>
</html>
