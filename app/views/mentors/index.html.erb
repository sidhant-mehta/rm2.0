<h3> </h3>

 <div class="grid_24">
<h3>Mentors - External</h3>
	<span class="initial_text">Mentors provide a reflective space and guidance on careers to the mentee; helping them to achieve their aspirations. <br /> 
		Use the search facility to find a mentor at an external organisation.
</span>

<%= render :partial => "shared/searchbar"%>
</div>
<% @mentors.each do |mentor| %>
<table class="table">
	<tr>
		<th class="mentor_title"><%=link_to "#{mentor.fname} #{mentor.lname}", mentor%></th>
		<th><%= image_tag(mentor.image.to_s , :class => "mentor_image" ) %></th> 	
		<td>
			<table class="mentor_inner">
				<tr><th>Organisation</th><th>Role</th><th>Sectors</th><th>Closing Date</th></tr>
				<tr><td><%= mentor.organisation%></td><td><%= mentor.role_title %></td><td><%=sectorsHTML(mentor)%></td><td><%=mentor.closing_date%></td></tr>
			</table>
		</td>
	</tr>
</table>
<%end%>

